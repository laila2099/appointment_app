class WalletsModel {
  final String title;
  final String icon;
  final String number;
  final bool isConnected;

  WalletsModel({
    required this.title,
    required this.icon,
    required this.number,
    required this.isConnected,
  });
}
